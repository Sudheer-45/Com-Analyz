# ai_service/requirements.txt

# --- Web Framework ---
Flask==2.3.3
Flask-Cors==4.0.0

# --- Audio and Image Processing ---
SpeechRecognition==3.10.1
pydub==0.25.1
opencv-python==4.8.0.76

# --- Core AI & ML Dependencies (PINNED FOR COMPATIBILITY) ---
# We pin protobuf to a version < 5.0 to satisfy tensorflow
protobuf==4.25.3
# We pin typing-extensions to a version < 4.6.0 to satisfy tensorflow
typing-extensions==4.5.0
# Tensorflow itself
numpy==1.23.5
tensorflow-intel==2.13.0
keras==2.13.1

# --- Main AI Libraries ---
deepface==0.0.79
# We add the new Google AI library here, it will use the compatible packages above
google-generativeai
flask
tensorflow  
opencv-python-headless
numpy